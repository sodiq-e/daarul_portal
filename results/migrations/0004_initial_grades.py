# Generated by Django 4.2.7 on 2026-04-19 20:52

from django.db import migrations


def create_initial_grades(apps, schema_editor):
    """Create initial grade scales"""
    GradeScale = apps.get_model('results', 'GradeScale')

    # Create default grade scale - each grade is a separate record
    grade_scale_name = 'Default Scale'

    grades_data = [
        (90, 100, 'A', 'Excellent', 5.0),
        (80, 89, 'B', 'Very Good', 4.0),
        (70, 79, 'C', 'Good', 3.0),
        (60, 69, 'D', 'Fair', 2.0),
        (50, 59, 'E', 'Poor', 1.0),
        (0, 49, 'F', 'Fail', 0.0),
    ]

    for min_score, max_score, grade, remark, grade_point in grades_data:
        GradeScale.objects.get_or_create(
            name=grade_scale_name,
            grade=grade,
            defaults={
                'min_score': min_score,
                'max_score': max_score,
                'remark': remark,
                'grade_point': grade_point
            }
        )


class Migration(migrations.Migration):

    dependencies = [
        ('results', '0003_gradescale_resulttemplate_studentresult_termresult_and_more'),
    ]

    operations = [
        migrations.RunPython(create_initial_grades),
    ]
